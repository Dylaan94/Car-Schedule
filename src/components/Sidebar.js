import React from "react";
import SidebarStyles from "./styles/SidebarStyles"

const Sidebar = (props) => {
  return <SidebarStyles.Sidebar>this is a sidebar</SidebarStyles.Sidebar>;
};

export default Sidebar;
